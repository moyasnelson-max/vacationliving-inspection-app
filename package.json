{
  "name": "vacationliving-inspection-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",

  "scripts": {
    "dev": "next dev",
    "build": "npm run guard && next build",
    "start": "next start",
    "lint": "next lint",

    /* ---- Protección permanente ---- */
    "guard": "node scripts/path-guardian.js",

    /* ---- Scripts de mantenimiento opcional ---- */
    "clean": "rm -rf .next node_modules && npm install",
    "validate": "bash full-validator.sh",
    "fix-all": "bash auto-fix-master-v8.6.sh"
  },

  "dependencies": {
    "@supabase/supabase-js": "2.39.3",
    "next": "14.0.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },

  "devDependencies": {
    "eslint": "8.56.0",
    "eslint-config-next": "14.0.3"
  }
}